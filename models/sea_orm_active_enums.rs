#[derive(Debug, Clone, PartialEq, Eq)]

pub enum ChildPole {
    
    N,
    
    S,
    
    E,
    
    W,
}
#[derive(Debug, Clone, PartialEq, Eq)]

pub enum ParentPole {
    
    N,
    
    S,
    
    E,
    
    W,
}
#[derive(Debug, Clone, PartialEq, Eq)]

pub enum Type {
    
    Direct,
    
    Loopback,
}
